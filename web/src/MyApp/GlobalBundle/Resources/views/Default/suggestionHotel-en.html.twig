{% block suggestionHotel %}
	<div class="suggestion ">
	  <h3>{{ "suggestion.hebergement" | trans }}</h3>
	  
	  {# Région 1 #}
		<div >
		   {% for r1 in displayregionLeft1 %}		   	   
			   <h2><a href="{{ path('_hebergementtyperegionen',{'name':categorie.repertoire_en|lower , 'region':r1.repertoireen|lower}) }}" title="Hotel{% if listClientGauche1|length > 1 %}s{% endif %} region {{ r1.nomen }}">Hotel{% if listClientGauche1|length > 1 %}s{% endif %} {{ r1.nomen }}</a></h2>
			         <ul>
			         {% for qc in listClientGauche1 %}
			           <li><a href="{{ path('_hebergementinfoclienten',{'name':qc.repertoireen|lower}) }}" target="_blank" title="Hotel {{ qc.nomen }}">{{ qc.nomen }}</a> {% if qc.prixapartir is not empty %} <span>{{ "a.partir" | trans }} {{ qc.prixapartir|replace({"$":""}) }}$</span>{% endif %}</li>
			         {% endfor %}
			         </ul>
			   <h4>[<a href="{{ path('_hebergementtyperegionen',{'name':categorie.repertoire_en|lower , 'region':r1.repertoireen|lower}) }}" title="Hotels region {{ r1.nomen }}" >Show hotels region {{ r1.nomen }}</a>]</h4>
		  {% endfor %}
		</div>
		
	   {# Région 2 #}
		<div >
		   {% for r2 in displayregionLeft2 %}		   	   
			   <h2><a href="{{ path('_hebergementtyperegionen',{'name':categorie.repertoire_en|lower , 'region':r2.repertoireen|lower}) }}" title="Hotel{% if listClientGauche2|length > 1 %}s{% endif %} region {{ r2.nomen }}">Hotel{% if listClientGauche2|length > 1 %}s{% endif %} {{ r2.nomen }}</a></h2>
			         <ul>
			         {% for qc in listClientGauche2 %}
			           <li><a href="{{ path('_hebergementinfoclienten',{'name':qc.repertoireen|lower}) }}" target="_blank" title="Hotel {{ qc.nomen }}">{{ qc.nomen }}</a>  {% if qc.prixapartir is not empty %}  <span>{{ "a.partir" | trans }} {{ qc.prixapartir|replace({"$":""}) }}$</span>{% endif %}</li>
			         {% endfor %}
			         </ul>
			   <h4>[<a href="{{ path('_hebergementtyperegionen',{'name':categorie.repertoire_en|lower , 'region':r2.repertoireen|lower}) }}" title="Hotels region {{ r2.nomen }}" >Show hotels region {{ r2.nomen }}</a>]</h4>
		  {% endfor %}
		</div>
		
		{# Région 3 #}
		<div >
		   {% for r3 in displayregionLeft3 %}		   	  
			   <h2><a href="{{ path('_hebergementtyperegionen',{'name':categorie.repertoire_en|lower , 'region':r3.repertoireen|lower}) }}" title="Hotel{% if listClientGauche3|length > 1 %}s{% endif %} region {{ r3.nomen }}">Hotel{% if listClientGauche3|length > 1 %}s{% endif %} {{ r3.nomen }}</a></h2>
			         <ul>
			         {% for qc in listClientGauche3 %}
			           <li><a href="{{ path('_hebergementinfoclienten',{'name':qc.repertoireen|lower}) }}" target="_blank" title="Hotel {{ qc.nomen }}">{{ qc.nomen }}</a> {% if qc.prixapartir is not empty %}  <span>{{ "a.partir" | trans }} {{ qc.prixapartir|replace({"$":""}) }}$</span>{% endif %}</li>
			         {% endfor %}
			         </ul>
			   <h4>[<a href="{{ path('_hebergementtyperegionen',{'name':categorie.repertoire_en|lower , 'region':r3.repertoireen|lower}) }}" title="Hotels region {{ r3.nomen }}" >Show hotels region {{ r3.nomen }}</a>]</h4>
		  {% endfor %}
		</div>
		
		{# Région 4 #}
		<div >
		   {% for r4 in displayregionLeft4 %}		   	   
			   <h2><a href="{{ path('_hebergementtyperegionen',{'name':categorie.repertoire_en|lower , 'region':r4.repertoireen}) }}" title="Hotel{% if listClientGauche4|length > 1 %}s{% endif %} region {{ r4.nomen }}">Hotel{% if listClientGauche4|length > 1 %}s{% endif %} {{ r4.nomen }}</a></h2>
			         <ul>
			         {% for qc in listClientGauche4 %}
			           <li><a href="{{ path('_hebergementinfoclienten',{'name':qc.repertoireen|lower}) }}" target="_blank" title="Hotel {{ qc.nomen }}">{{ qc.nomen }}</a> {% if qc.prixapartir is not empty %} <span>{{ "a.partir" | trans }} {{ qc.prixapartir|replace({"$":""}) }}$</span>{% endif %}</li>
			         {% endfor %}
			         </ul>
			   <h4>[<a href="{{ path('_hebergementtyperegionen',{'name':categorie.repertoire_en|lower , 'region':r4.repertoireen|lower}) }}" title="Hotels region {{ r4.nomen }}" >Show hotels region {{ r4.nomen }}</a>]</h4>
		  {% endfor %}
		</div>
	   <br class="clearfloat" />
	 </div>
{% endblock %}