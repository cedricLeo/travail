{% extends '::base1.html.twig' %}
{% block content %}
	<div class="tabAdmin">	
	
		<table align="center" width="100%" cellspacing="0" cellpadding="0" border="0" class="template_etroit">
			<tbody>
				<tr class="rowheader">
					<td><a href="{{path('_Dashboard_addcategoriehebergement')}}" class="addpays"><img src="{{ asset('bundles/myappadmin/images/Button Add.png') }}" class="add_ville" />Ajouter une catégorie d'hébergement</a>
						<table align="center" width="550" cellspacing="3" cellpadding="0" border="0">
							<tbody>
									<tr class="rowsubheader">
										<td width="270" colspan=1>Nom fr</td>
										<td width="270">Nom en</td>
									</tr>
								{% for p in categorie %}
									<tr>
										<td colspan=1><a href="{{path('_Dashboard_updatecategoriehebergement',{'id':p.id})}}">{# {% if p.image is defined %}<img src="{{ asset('uploads/categorie_hebergement') }}/{{ p.id }}/{{ p.image }}" class="reajuste_img" />{% endif %}#}{{ p.nom_fr }}</a></td>
										<td colspan=1><a href="{{path('_Dashboard_updatecategoriehebergement',{'id':p.id})}}">{{ p.nom_en }}</a></td>
									</tr>
								{% endfor %}
								{% if categorie is empty %}
										<p class="searchnull">Pas de résultat trouvé</p>
								{% endif %}		
							</tbody>
						</table>
					</td>
				</tr>
			</tbody>
		</table>
	</div>
{% endblock %}